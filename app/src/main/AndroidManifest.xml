<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/dartbud_logo_placeholder"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dartbud_logo_placeholder_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DartBud">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DartBud"
            android:screenOrientation="portrait">
            <!-- Ingen skjerm i appen har et landskaps-oppsett - alt er faste vertikale
                 Column-layouter (spillerkort, tastatur, osv.). Uten denne låsen kunne en
                 rotasjon midt i en kamp trigge en Activity-omstart som nullstiller hele
                 GameScreen sin tilstand (gameState, ventende checkout-dialog osv.), siden
                 den kun holdes i remember{}, ikke rememberSaveable/ViewModel. -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>